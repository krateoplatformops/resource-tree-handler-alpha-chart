{{- if .Values.webservice.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name:  {{ include "resource-tree-handler.fullname" . }}
  labels:
    {{- include "resource-tree-handler.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "resource-tree-handler.fullname" . }}
  minReplicas: {{ .Values.webservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.webservice.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.webservice.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.webservice.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.webservice.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.webservice.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
